# Obsolete placeholder.
#
# Do not deploy this file. The active native UI/runtime path uses
# llm_semantic_route.tcl, deployed as /Common/llm_semantic_route_phase2.
#
# This file used to contain an early static demo iRule with fixed backend
# defaults. It is intentionally left non-deployable to avoid reintroducing
# hidden fallback routing or sample-model assumptions.
